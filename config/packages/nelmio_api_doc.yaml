nelmio_api_doc:
    documentation:
        info:
            title: SnapStamp API
            description: Digital loyalty rewards platform API
            version: 1.0.0
        components:
            securitySchemes:
                bearerAuth:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
        security:
            - bearerAuth: []
    areas:
        path_patterns:
            - ^/api/v1
